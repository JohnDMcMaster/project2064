Writes all permutations of the LUT in "Editblk FF"

Result
Bits frame and offset varying according to location but stay relative
Memory is inverted. That is:
Equate F = ((~A)*(~B)*(~C)*(~D))
Clears a bit in the output

note: 0000 doesn't seem to generate correctly

